services:
  - type: web
    name: mobile-pos-tomorrow
    env: docker
    plan: free
    autoDeploy: false
    healthCheckPath: /api/status
    envVars:
      - key: DATA_DIR
        value: /tmp/pos-data
      - key: COOKIE_SECURE
        value: "1"
